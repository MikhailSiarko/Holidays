version: '3.9'

services:
  api:
    container_name: holidays.api
    image: ${DOCKER_REGISTRY-}holidays.api
    build:
      context: .
      dockerfile: src/Holidays.API/Dockerfile
    depends_on: 
      - db
  
  db:
    image: "mcr.microsoft.com/mssql/server:2019-latest"
    container_name: holidays.db
